[project]
name = "home-enquiry-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["flask",  "pandas", "google-api-python-client", "waitress", "flask-sqlalchemy", "flask-session" ]
